# auto selects CUDA when available, otherwise CPU.
MODEL_DEVICE=auto

# Optional overrides; relative paths are resolved from the process working directory.
# MODEL_PATH=D:\Study\MLOPS\artifacts\bioactivity_mlp_scripted.pt
# MODEL_METADATA_PATH=D:\Study\MLOPS\artifacts\model_metadata.json

# Comma-separated front-end origins. Empty means CORS middleware is disabled.
# CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
